<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <!--
        This POM exists as a convenience to build all modules in the LvvService project.
        https://maven.apache.org/guides/mini/guide-multiple-modules.html
    -->
  <parent>
    <groupId>com.oracle.pic.commons</groupId>
    <artifactId>service-pom</artifactId>
    <version>2.0.16</version>
  </parent>

  <modelVersion>4.0.0</modelVersion>

  <groupId>com.oracle.pic.networking.lvv.service</groupId>
  <artifactId>lvv-service-reactor-pom</artifactId>
  <name>LvvService Reactor Pom</name>
  <version>0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <modules>
    <module>lvv-service-spec</module>
    <module>lvv-service-java-client</module>
    <module>lvv-service-python-client</module>
    <module>lvv-service-api</module>
    <module>lvv-service-canary</module>
  </modules>

  <properties>
    <!-- Treat any resources/sources as UTF-8 -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <nwauto-core-bom-version>0.1.65</nwauto-core-bom-version>

    <formatter-goal>apply</formatter-goal>
    <maven-toolchains-plugin-version>3.0.0</maven-toolchains-plugin-version>
  </properties>

  <!--
Note: we need to mention repositories to look up as there is a bug in maven where it does not look at all places if parent bom and parent's parent bom has different locations.
Slack thread: https://dyn.slack.com/archives/CACSG1M4Z/p1633120108307300
Maven ticket: https://issues.apache.org/jira/browse/MNG-4148
-->
  <repositories>
    <repository>
      <id>_my_central_commons</id>
      <name>libs-release_commons</name>
      <url>https://artifactory.oci.oraclecorp.com/libs-release</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>_my_snapshot_commons</id>
      <name>libs-snapshot_commons</name>
      <url>https://artifactory.oci.oraclecorp.com/libs-snapshot</url>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>
      <!-- NA Internal BOM containing dropwizard and oci internal bom -->
      <dependency>
        <groupId>com.oracle.pic.networking.commons</groupId>
        <artifactId>nwauto-core-bom</artifactId>
        <version>${nwauto-core-bom-version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Publications from the current project's sub-modules -->
      <dependency>
        <groupId>com.oracle.pic.networking.lvv.service</groupId>
        <artifactId>lvv-service-spec</artifactId>
        <version>0.1-SNAPSHOT</version>
      </dependency>
      <dependency>
        <groupId>com.oracle.pic.networking.lvv.service</groupId>
        <artifactId>lvv-service-java-client</artifactId>
        <version>0.1-SNAPSHOT</version>
      </dependency>

      <!-- Atlassian dependencies -->
      <dependency>
        <groupId>com.atlassian.jira</groupId>
        <artifactId>jira-rest-java-client-api</artifactId>
        <version>5.1.6</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.jira</groupId>
        <artifactId>jira-rest-java-client-core</artifactId>
        <version>5.1.6</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.jira</groupId>
        <artifactId>jira-api</artifactId>
        <version>8.22.4</version>
        <!-- Security Exclusion related to https://nvd.nist.gov/vuln/detail/CVE-2020-15227 -->
        <exclusions>
          <exclusion>
            <groupId>com.atlassian.application</groupId>
            <artifactId>atlassian-application-api</artifactId>
          </exclusion>
        </exclusions>
      </dependency>

      <!-- Atlassian fix enforcer errors -->
      <dependency>
        <groupId>com.atlassian.event</groupId>
        <artifactId>atlassian-event</artifactId>
        <version>2.3.2</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.fugue</groupId>
        <artifactId>fugue</artifactId>
        <version>2.6.1</version>
      </dependency>
      <dependency>
        <groupId>commons-lang</groupId>
        <artifactId>commons-lang</artifactId>
        <version>2.6</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.core</groupId>
        <artifactId>atlassian-core</artifactId>
        <version>7.0.0</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.annotations</groupId>
        <artifactId>atlassian-annotations</artifactId>
        <version>0.14</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.util.concurrent</groupId>
        <artifactId>atlassian-util-concurrent</artifactId>
        <version>2.4.0</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.sal</groupId>
        <artifactId>sal-api</artifactId>
        <version>2.10.20</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.analytics</groupId>
        <artifactId>analytics-api</artifactId>
        <version>6.3.0</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.plugins</groupId>
        <artifactId>atlassian-plugins-webfragment</artifactId>
        <version>5.3.2</version>
      </dependency>
      <dependency>
        <groupId>com.atlassian.plugins</groupId>
        <artifactId>atlassian-plugins-webfragment-api</artifactId>
        <version>5.3.2</version>
      </dependency>
      <dependency>
        <groupId>org.codehaus.jettison</groupId>
        <artifactId>jettison</artifactId>
        <version>1.5.4</version>
      </dependency>

      <!-- NCP & Mockito-->
      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>4.11.0</version>
      </dependency>
      <dependency>
        <groupId>com.oracle.pic.networking.ncp</groupId>
        <artifactId>ncp-api-java-public-client</artifactId>
        <version>1.1.1988</version>
        <exclusions>
          <exclusion>
            <groupId>com.oracle.pic.telemetry.dianoga</groupId>
            <artifactId>dianoga-service-java-client</artifactId>
          </exclusion>
          <exclusion>
            <groupId>com.oracle.pic.identity.authorization</groupId>
            <artifactId>sdk</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.0.0</version>
        <inherited>false</inherited>
        <executions>
          <execution>
            <id>Remove old BOM update</id>
            <phase>validate</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>rm</executable>
              <arguments>
                <argument>${project.basedir}/bom-dependency-versions.txt</argument>
              </arguments>
              <successCodes>
                <!-- Don't fail if the file isn't there -->
                <successCode>0</successCode>
                <successCode>1</successCode>
              </successCodes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!-- This plugin will log the latest version available of the specified dependencies, and if they are being used
           or not. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>versions-maven-plugin</artifactId>
        <version>2.8.1</version>
        <executions>
          <execution>
            <phase>install</phase>
            <goals>
              <goal>display-property-updates</goal>
            </goals>
          </execution>
        </executions>

        <!-- We only want to run this plugin in this pom, and not every module. -->
        <inherited>false</inherited>
      </plugin>

    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.10.1</version>
          <configuration>
            <source>17</source>
            <target>17</target>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <profiles>
    <profile>
      <id>enforce-project-version</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-enforcer-plugin</artifactId>
            <version>1.4.1</version>
            <dependencies>
              <dependency>
                <groupId>com.oracle.pic.kiev.maven</groupId>
                <artifactId>maven-enforcer-rules</artifactId>
                <version>1.0.5</version>
              </dependency>
            </dependencies>
            <executions>
              <execution>
                <id>requireProjectVersion</id>
                <goals>
                  <goal>enforce</goal>
                </goals>
                <configuration>
                  <rules>
                    <requireProjectVersion implementation="com.oracle.pic.kiev.maven.enforcer.RequireProjectVersion">
                      <requiredVersion>${env.REQUIRED_PROJECT_VERSION}</requiredVersion>
                    </requireProjectVersion>
                  </rules>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <!-- If there are Java sources, apply checkstyle, pmd, findbugs, code-coverage, etc. -->
    <profile>
      <id>profile-java-has-main-sources</id>
      <activation>
        <activeByDefault>false</activeByDefault>
        <file>
          <exists>${basedir}/src/main/java</exists>
        </file>
      </activation>
      <build>
        <pluginManagement>
          <plugins>
            <!--This plugin's configuration is used to store Eclipse m2e settings
              only. It has no influence on the Maven build itself. -->
            <plugin>
              <groupId>org.eclipse.m2e</groupId>
              <artifactId>lifecycle-mapping</artifactId>
              <version>1.0.0</version>
              <configuration>
                <lifecycleMappingMetadata>
                  <pluginExecutions>
                    <pluginExecution>
                      <pluginExecutionFilter>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <versionRange>[1.9.1,)</versionRange>
                        <goals>
                          <goal>parse-version</goal>
                        </goals>
                      </pluginExecutionFilter>
                      <action>
                        <ignore/>
                      </action>
                    </pluginExecution>
                  </pluginExecutions>
                </lifecycleMappingMetadata>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>

        <plugins>
          <!-- Run dependency analyzer during build to keep your dependency graph under control. -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <version>3.4.0</version>
            <executions>
              <execution>
                <id>analyze-dependencies</id>
                <goals>
                  <goal>analyze-only</goal>
                </goals>
                <configuration>
                  <ignoredUnusedDeclaredDependencies>
                    <ignoredUnusedDeclaredDependency>javax.validation</ignoredUnusedDeclaredDependency>
                    <ignoredUnusedDeclaredDependency>javax.servlet</ignoredUnusedDeclaredDependency>
                    <ignoredUnusedDeclaredDependency>javax.ws.rs</ignoredUnusedDeclaredDependency>
                  </ignoredUnusedDeclaredDependencies>

                  <ignoredUsedUndeclaredDependencies>
                    <ignoredUsedUndeclaredDependency>jakarta.ws.rs</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>com.oracle.pic.kiev</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>javax.inject</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>io.dropwizard:dropwizard-servlets</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>jakarta.servlet</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>jakarta.validation</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>org.glassfish.hk2.external</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>com.oracle.pic.authproxy</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>com.oracle.oci.sdk</ignoredUsedUndeclaredDependency>
                    <ignoredUsedUndeclaredDependency>junit</ignoredUsedUndeclaredDependency>
                  </ignoredUsedUndeclaredDependencies>

                  <failOnWarning>false</failOnWarning>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- Enforce various best-practices during build. -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-enforcer-plugin</artifactId>
            <version>3.1.0</version>
            <executions>
              <execution>
                <id>enforce</id>
                <configuration>
                  <rules>
                    <!-- https://maven.apache.org/enforcer/enforcer-rules/dependencyConvergence.html -->
                    <DependencyConvergence/>
                    <!-- https://maven.apache.org/enforcer/enforcer-rules/requireJavaVersion.html -->
                    <requireJavaVersion>
                      <version>17</version>
                    </requireJavaVersion>
                    <!-- https://www.mojohaus.org/extra-enforcer-rules/banDuplicateClasses.html -->
                    <banDuplicateClasses/>
                    <!-- Ban all non FIPS bouncy castle use -->
                    <bannedDependencies>
                      <searchTransitive>true</searchTransitive>
                      <excludes>
                        <exclude>org.bouncycastle:*</exclude>
                      </excludes>
                    </bannedDependencies>
                  </rules>
                </configuration>
                <goals>
                  <goal>enforce</goal>
                </goals>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-checkstyle-plugin</artifactId>
            <version>3.1.1</version>
            <configuration>
              <configLocation>../checkstyle.xml</configLocation>
              <encoding>UTF-8</encoding>
              <consoleOutput>true</consoleOutput>
              <failsOnError>true</failsOnError>
              <linkXRef>false</linkXRef>
              <violationSeverity>warning</violationSeverity>
              <suppressionsLocation>build_config/checkstyle-suppressions.xml</suppressionsLocation>
            </configuration>
            <executions>
              <execution>
                <id>verify-style</id>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>verify</phase>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-pmd-plugin</artifactId>
            <version>3.12.0</version>
            <configuration>
              <linkXRef>false</linkXRef>
              <sourceEncoding>UTF-8</sourceEncoding>
              <excludeRoots>
                <excludeRoot>target/generated-sources</excludeRoot>
              </excludeRoots>
              <!-- duplicate code minimumTokens default: 100 -->
              <minimumTokens>100</minimumTokens>
              <excludeFromFailureFile>${project.basedir}/build_config/exclude-pmd.properties</excludeFromFailureFile>
              <printFailingErrors>true</printFailingErrors>
            </configuration>
            <executions>
              <execution>
                <goals>
                  <goal>check</goal>
                  <goal>cpd-check</goal>
                </goals>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-maven-plugin</artifactId>
            <version>4.2.2</version>
            <configuration>
              <effort>Max</effort>
              <xmlOutput>true</xmlOutput>
              <excludeFilterFile>build_config/findbugs-exclude.xml</excludeFilterFile>
              <plugins>
                <plugin>
                  <groupId>jp.skypencil.findbugs.slf4j</groupId>
                  <artifactId>bug-pattern</artifactId>
                  <version>1.4.2</version>
                </plugin>
              </plugins>
            </configuration>
            <executions>
              <execution>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>verify</phase>
              </execution>
            </executions>
          </plugin>

          <!-- Spotless is a general-purpose formatting plugin. It is completely Ã la carte,
          but also includes powerful "batteries-included" if you opt-in
          https://github.com/diffplug/spotless/tree/master/plugin-maven -->
          <plugin>
            <groupId>com.diffplug.spotless</groupId>
            <artifactId>spotless-maven-plugin</artifactId>
            <version>2.39.0</version>
            <configuration>
              <java>
                <removeUnusedImports/>
                <googleJavaFormat>
                  <version>1.17.0</version>
                  <!-- Optional, available versions: GOOGLE, AOSP
                  https://github.com/google/google-java-format/blob/master/core/src/main/java/com/google/googlejavaformat/java/JavaFormatterOptions.java -->
                  <style>AOSP</style>
                </googleJavaFormat>
              </java>
            </configuration>
            <executions>
              <execution>
                <id>format-sources</id>
                <goals>
                  <goal>${formatter-goal}</goal>
                </goals>
                <phase>process-sources</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!-- Use the 'dev' profile to see stdout from unit tests in the console -->
    <profile>
      <id>dev</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <disableXmlReport>true</disableXmlReport>
              <redirectTestOutputToFile>false</redirectTestOutputToFile>
              <useFile>false</useFile>
              <trimStackTrace>false</trimStackTrace>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!--
        The following are profiles to add the exceptions for the respective tools, if the exception file exists.
        These rely on activation based on file presence, due to which, the paths cannot be referenced using arbitrary
        properties (only basedir and a few others may be used). Hence, the paths to the files are duplicated.
    -->
    <profile>
      <id>profile-checkstyle-exception-exists</id>
      <activation>
        <activeByDefault>false</activeByDefault>
        <file>
          <exists>build_config/checkstyle-suppressions.xml</exists>
        </file>
      </activation>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-checkstyle-plugin</artifactId>
              <configuration>
                <suppressionsLocation>${basedir}/build_config/checkstyle-suppressions.xml</suppressionsLocation>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>

    <!-- In Build Service we want to fail if the code isn't formatted -->
    <profile>
      <id>in-build-service</id>
      <activation>
        <activeByDefault>false</activeByDefault>
        <property>
          <name>env.BLD_GUID</name>
        </property>
      </activation>
      <properties>
        <formatter-goal>check</formatter-goal>
      </properties>
    </profile>
  </profiles>
</project>
